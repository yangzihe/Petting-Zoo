<%= form_for @pen, :url => pen_create_path do |p| %>
  <p>
    <%= p.label :name, "Pen Name" %>
    <%= p.text_field :name %>
  </p>
  <p>
    <%= p.label :capacity, "Pen Capacity" %>
    <%= p.text_field :capacity %>
  </p>
  <p>
    <%= p.submit "Create a new Pen!" %>
  </p>
<% end %>